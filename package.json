{
  "name": "blog",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "serve": "npx @11ty/eleventy --serve",
    "start": "npm run serve",
    "deploy": "npm run clean && npx @11ty/eleventy --pathprefix 'personal-blog' && gh-pages -d _site",
    "clean": "rm -rf _site"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@11ty/eleventy": "^3.0.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
